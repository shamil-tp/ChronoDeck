<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Tic-Tac-Toe (3×3 & 5×5)</title>

  <!-- Bootstrap 5 -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
    crossorigin="anonymous"
  />
  <link rel="stylesheet" href="./styles/style.css">
  <style>
    :root {
      --cell-size-3: 108px;   /* 3×3 big cells */
      --cell-size-5: 72px;    /* 5×5 slightly smaller cells */
      --cell-gap: 10px;
    }

    body { background: #0f172a; } /* slate-900 */
    .game-card {
      border: 0;
      border-radius: 1.25rem;
      box-shadow: 0 20px 40px rgba(0,0,0,.25);
    }

    /* Board uses CSS Grid; size switches based on selection */
    #board {
      display: grid;
      gap: var(--cell-gap);
      justify-content: center;
      align-content: center;
      user-select: none;
      transition: transform .2s ease;
    }
    #board.size-3 {
      grid-template-columns: repeat(3, var(--cell-size-3));
      grid-template-rows: repeat(3, var(--cell-size-3));
    }
    #board.size-5 {
      grid-template-columns: repeat(5, var(--cell-size-5));
      grid-template-rows: repeat(5, var(--cell-size-5));
    }
    .cell {
      background: #111827;            /* gray-900 */
      border-radius: 1rem;
      display: grid;
      place-items: center;
      font-weight: 700;
      letter-spacing: .5px;
      cursor: pointer;
      transition: transform .08s ease, background .15s ease, box-shadow .15s ease;
      box-shadow: inset 0 0 0 2px rgba(255,255,255,.04);
    }
    .cell:hover { transform: translateY(-2px); background: #0b1220; }
    .cell:active { transform: translateY(0); }

    .cell.x { color: #60a5fa; text-shadow: 0 4px 14px rgba(96,165,250,.4); } /* blue-400 */
    .cell.o { color: #f472b6; text-shadow: 0 4px 14px rgba(244,114,182,.4); } /* pink-400 */

    .cell.winning {
      outline: 3px solid #22c55e;     /* green-500 */
      box-shadow: 0 0 0 3px rgba(34,197,94,.25);
      background: #052e1b;
    }

    .badge-mode { font-size: .8rem; }

    /* Responsive tweak: scale board on small screens */
    @media (max-width: 420px) {
      #board { transform: scale(.9); }
    }
  </style>
</head>
<body>
  <div class="container py-5">
    <div class="mx-auto" style="max-width: 760px;">
      <div class="card game-card bg-dark text-light">
        <div class="card-body p-4 p-md-5">
          <div class="d-flex flex-wrap align-items-center gap-2 justify-content-between mb-4">
            <div class="d-flex align-items-center gap-3">
              <h1 class="h3 mb-0">Tic-Tac-Toe</h1>
              <span id="modeBadge" class="badge bg-secondary badge-mode">3×3</span>
            </div>

            <div class="d-flex flex-wrap gap-2">
              <div class="input-group">
                <label class="input-group-text bg-secondary text-light border-0" for="sizeSelect">Board</label>
                <select id="sizeSelect" class="form-select bg-dark text-light border-secondary">
                  <option value="3" selected>3 × 3</option>
                  <option value="5">5 × 5</option>
                </select>
              </div>
              <button id="resetBtn" class="btn btn-outline-light">Reset</button>
            </div>
          </div>

          <p id="status" class="lead mb-4">Turn: <strong>X</strong></p>

          <div id="board" class="size-3" aria-label="Tic Tac Toe Board"></div>

          <div class="mt-4 d-flex gap-3">
            <div class="text-muted small">Tip: Click a cell to place your mark.</div>
          </div>
        </div>
      </div>
    </div>
  </div>
  <script src="./script/script.js"></script>
</body>
</html>
